"""
StringReverser

Reverse a string or text

Auto-generated by LLM Agent Framework
Generated at: 2025-11-12 21:40:42
"""

import sys
import os
from typing import Dict, Any

# Add parent directory to path
sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from core.tool import Tool

from typing import Dict, Any

class Tool:
    def __init__(self, name: str, description: str):
        self.name = name
        self.description = description
    
    def execute(self, **kwargs) -> Dict[str, Any]:
        raise NotImplementedError

class StringReverser(Tool):
    def __init__(self):
        super().__init__(name="StringReverser", description="Reverse a string or text")

    def execute(self, text: str) -> Dict[str, Any]:
        """
        Reverses the provided string.

        Parameters:
        text (str): The text to reverse.

        Returns:
        Dict[str, Any]: A dictionary containing the reversed string.
        
        Raises:
        ValueError: If the input text is not a string.
        """
        if not isinstance(text, str):
            raise ValueError("Input must be a string.")

        reversed_text = text[::-1]
        return {"reversed_text": reversed_text}